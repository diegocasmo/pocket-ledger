## Summary

<!-- 1 to 3 sentences: what this PR does and why. Be concise; don't restate what the CI checks already show (passing tests, lint, build). -->

<!--
Optional sections. Add any that add signal, then delete this comment:

  ## Root cause  (for fixes: what actually caused the bug)
  ## Evidence    (proof the CI checks can't show: repro steps, before/after, screenshots)
  ## Scope       (notable changes and anything deliberately out of scope)
-->

Closes #
